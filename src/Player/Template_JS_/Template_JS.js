var test = 0;

var GetName = function () {
    return "JSPlayer";
}

var Initialize = function (myNumber, col, row) {
}

var Process = function (playerNumber, playerPosition, mapBlocks) {
    return test++ % 4;
}